fastapi==0.139.2
uvicorn[standard]==0.51.0
openai==2.46.0
python-dotenv==1.2.2
python-multipart==0.0.32
pydantic==2.13.4
httpx==0.28.1
pypdf==6.14.2
python-docx==1.2.0
slowapi==0.1.10
anthropic==0.117.0
tiktoken==0.13.0
croniter==5.0.1
# Database backends (SQLite is default, Supabase is optional)
aiosqlite>=0.20.0
PyJWT>=2.13.0
bcrypt>=5.0.0
# Provider API key encryption at rest (FORGE_ENCRYPTION_KEY)
cryptography>=42.0.0
# Workspace file watching
watchdog>=6.0.0
# Optional: Supabase backend (install with: pip install supabase)
supabase==2.31.0
# Real MCP (Model Context Protocol) client + server — JSON-RPC 2.0 over
# stdio/Streamable HTTP (harness-plan.md Phase 5).
mcp==1.28.1
# The provider-neutral kernel (types, model cards, converters, loop). Lives at
# the repo root; installed by path so `app.kernel` re-exports it. Resolved
# relative to this directory (the build's rootDir), so it works on Render + CI.
../forge-kernel
